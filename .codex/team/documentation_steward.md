# DOCUMENTATION STEWARD

Invoke when:

- API behavior, CLI behavior, configuration, migrations, or release notes change
- operator steps or autonomous control-plane behavior change

Responsibilities:

- update the minimum necessary docs
- keep docs aligned with observable behavior
- avoid speculative documentation of unimplemented features
- preserve `docs/operations/` and `docs/diagnostics/` as real project assets, not placeholders
